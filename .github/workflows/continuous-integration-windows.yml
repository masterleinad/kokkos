name: github-windows

on:
  workflow_call:
  push:

concurrency:
  group: ${ {github.event_name }}-${{ github.workflow }}-${{ github.ref }}-win-x64
  cancel-in-progress: ${{github.event_name == 'pull_request'}}

permissions: read-all

jobs:
  windows-cuda:
    # Cuda build on Windows
    name: Windows Cuda
    runs-on: windows-2022

    steps:
    - name: Support longpaths
      run: git config --system core.longpaths true
    - uses: Jimver/cuda-toolkit@8022558310ea543e35132143092835585f60e628 # v0.2.21
      id: cuda-toolkit
      with:
        cuda: '12.4.1'
        method: 'network'
        sub-packages: '["nvcc","cudart","thrust"]'
    - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
    - name: configure
      shell: bash
      run: |
        echo "$CUDA_PATH/bin" >> "$GITHUB_PATH"
        which nvcc
        mkdir build
        mkdir c:/project
        cd build
        cmake -DKokkos_ENABLE_CUDA=ON -DKokkos_ARCH_VOLTA70=ON -DKokkos_ENABLE_TESTS=ON -DKokkos_ENABLE_COMPILE_AS_CMAKE_LANGUAGE=ON -DCMAKE_CUDA_COMPILER="${CUDA_PATH}/bin/nvcc" ..
    - name: build library
      shell: bash
      run: |
        cmake --build build --parallel 2 --config Release
